{
  "name": "ruca",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph",
    "reset": "nx reset"
  },
  "packageManager": "yarn@4.2.2",
  "devDependencies": {
    "nx": "^19.2.0"
  }
}
